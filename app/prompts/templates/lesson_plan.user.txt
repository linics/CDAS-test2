Task: convert this lesson plan into a student assignment draft.
Follow constraints:
- objectives includes knowledge/process/emotion
- objectives.process must start with a background setting sentence prefixed by "背景设定：".
- the background setting must be a single short paragraph (about 80-140 Chinese characters), fit grade, and match the lesson plan theme.
- phases includes name/order/steps
- each phase should include a scenario title that naturally continues from previous phase
- total phase count must be 3-4 only (merge template phases if needed)
- each phase should contain 1 core step (max 2 steps)
- each step includes name/description/checkpoints
- each step must include content as a short narrative bridge tied to the background setting
- step.description should be a learning scaffold sentence with context and hint
- across the whole assignment, highlight 4-6 truly core steps; for any extra steps from the template, mark them optional by starting description with "（可选）".
- each description should start with a 1-sentence scene hook connected to the background setting, then give concrete guidance.
- each checkpoint includes content/evidence_type (text/document/image/video/confirm/link)
- rubric includes 5-6 dimensions with levels excellent/good/pass/improve
- keep progression from task understanding to evidence production and reflection
- avoid formulaic repetition; make students want to read and start immediately

Seed profile:
- title={title}
- topic={topic}
- school_stage={school_stage}
- grade={grade}
- assignment_type={assignment_type}
- inquiry_depth={inquiry_depth}
- submission_mode={submission_mode}
- duration_weeks={duration_weeks}
- main_subject={main_subject}
- related_subjects={related_subjects}

Lesson plan excerpt:
{lesson_plan_excerpt}

Structure template (keep compatible order/shape, enrich content):
{template_json}

Quality constraints:
- keep each step concise and practical for classroom execution
- each step checkpoints count should be 1-2 only
- avoid generic placeholders and repeated slogans
